#include "main.h"
/**
 * _islower - checks for lowercase characters
 * @c: the character to be checked
 * Return: return 1 if c is in lowercase, and 0 if not
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
